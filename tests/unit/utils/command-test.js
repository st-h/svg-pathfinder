import command from 'pathfinder/utils/command';
import { module, test } from 'qunit';

module('Unit | Utility | command', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = command();
    assert.ok(result);
  });
});
