import pathCommand from 'pathfinder/utils/path-command';
import { module, test } from 'qunit';

module('Unit | Utility | path-command', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = pathCommand();
    assert.ok(result);
  });
});
